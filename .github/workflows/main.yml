name: push_test 
on: push 
  jobs:
    start_up_jobs:
      runs-on: ubuntu-latest
      steps: 
        -name: use node.js 
         uses: actions/setup-node@v3 
         with: 
           node-version: '18.x'
         - run: npm install 
         - run: npm run build
